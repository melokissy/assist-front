import { AssistFormFieldDirective } from './assist-form-field.directive';

describe('AssistFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new AssistFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
